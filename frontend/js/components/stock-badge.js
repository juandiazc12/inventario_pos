/**
 * stock-badge.js — Badge de estado de stock (componente)
 */

// Ya definido en utils.js como renderStockBadge()
// Este archivo existe para compatibilidad con la estructura del proyecto

window.StockBadge = {
    render: renderStockBadge
};
